#include <cstdio>
#include <algorithm>
using namespace std;

const int MAX_N = 100;
const int MAX_a = MAX_m = 10000;
const int MAX_K = 100000;

int n,K;
int a[MAX_N], m[MAX_N];

bool dp[MAX_N + 1][MAX_K + 1];

void read(){
  scanf("%d\n", &n);

  for(int i=0; i<n; i++){
    scanf("%d", &a[i]);
  }
  for(int i=0; i<n; i++){
    scanf("%d", &m[i]);
  }

  scanf("%d\n", &K);

}


void solve(){

  int ans=0;
  printf("%d\n", ans);

}

int main(){
  read();
  solve();
  return 0;
}

