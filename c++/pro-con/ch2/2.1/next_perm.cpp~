bool used[MAX_N]:
int perm[MAX_N]:

void permutation1 (int pos, int n) {
  if (pos == n) {
    // do something on perm[]
    return;
  }

  for (int i=0; i<n; i++){
    if (!used[i]){
      perm[pos] = i;
      used[i]=true;
      permutation1(pos+1, n);
      used[i]=false;
    }
  }
  return;
}

